# Testing repository for email sending

To try this, run:

```
node send_mail.js
```

You will see an error because the password is wrong. But, notice the error says:

> Invalid login: 535 Authentication Failed

